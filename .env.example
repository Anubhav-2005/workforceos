# Required for accounts and shared workspace data.
DATABASE_URL=

# Optional direct PostgreSQL URL for deployment migrations (Neon supplies this).
DATABASE_URL_UNPOOLED=

# Required for AI resume analysis. Keep these server-side.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Public absolute URL used for canonical metadata, sitemap, and social cards.
NEXT_PUBLIC_APP_URL=
